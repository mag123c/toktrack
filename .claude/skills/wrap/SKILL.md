---
name: wrap
description: "Wrap toktrack changes using shared requirements and evidence with repository-specific quality gates."
---

# Wrap

Read `../../ai-context/workflow.md` relative to this skill directory. If installed, read `~/.claude/skills/wrap/SKILL.md`; otherwise use the repository workflow's portable fallback. Both Claude and Codex follow the same acceptance, verification, and authorization boundaries.

Preserve toktrack's TDD for implementation, `make check` before commit, source failure behavior, and commit conventions. A clear direct request can replace clarify. Continue only through authorized phases; merge, release, and worktree removal are separate actions.
